import { ResultReports } from "@/components/pages/reports/ResultReports";

export const metadata = {
  title: "Result Reports | ePathshala",
  description: "View student rankings, exam scores and result analytics.",
};

export default function Page() {
  return <ResultReports />;
}
