import { SubjectAssignment } from "@/components/pages/subject";

export const metadata = {
  title: "Subject Assignment | ePathshala",
  description: "Assign subjects to specific classes, sections and shifts.",
};

export default function Page() {
  return <SubjectAssignment />;
}
